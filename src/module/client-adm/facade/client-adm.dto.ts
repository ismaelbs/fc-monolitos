export type AddClientInputFacadeDto = {
    name: string;
    email: string;
    address: string;
}

export type AddClientOutFacadeDto = {
    id: string;
    name: string;
    email: string;
    address: string;
}